// Registers jest-dom matchers (e.g. `toBeInTheDocument`) on Vitest's `expect`
// and augments its types. Referenced by `vite.config.ts` -> test.setupFiles.
import "@testing-library/jest-dom/vitest";
