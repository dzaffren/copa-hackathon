# Base URL for the Workstream Brain FastAPI engine.
# Leave empty to hit same-origin (e.g. behind a dev proxy) or in tests where
# MSW intercepts the relative "/api/..." paths.
VITE_API_BASE=http://localhost:8000
