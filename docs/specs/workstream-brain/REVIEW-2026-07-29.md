# Demo-Readiness Review — 29 Jul 2026 (for your return)

**Branch:** `feat/demo-ready` (local only — **nothing pushed, nothing merged to
staging/main**). **Demo:** 3 Aug 2026. **Prepared autonomously while you were on a
flight, pending your review.**

---

## TL;DR

- **The demo renders end-to-end and cleanly.** I ran the app (engine + frontend) and
  walked all seven demo screens for `open-finance-pd-2026` — sidebar, graph, task
  screen, review (47 real findings, verbatim citations, five-label taxonomy),
  cross-intelligence, review-queue, new-workstream. **Zero console errors, zero failed
  requests.** Screenshots were captured.
- **The whole branch is green:** engine **500 passed / 0 failed**, frontend **132 passed
  / 0 failed**.
- **Your Tier 1 hardening plan is mostly moot now** — upstream `#52/#53` landed the
  "build a workstream from scratch" epic while we were working, which fixed the
  live-analyze gap a different (better) way. I pivoted from _building the plan_ to
  _reconciling with reality + verifying the demo_.
- **Two things need YOU:** (1) a go/no-go on merging this branch to `staging` (it fixes
  18 tests that are currently red on `staging`), and (2) rehearsing the _live_ paths
  (Analyze / Extract-concepts / Copilot) on a machine with Azure credentials — I can't
  reach a model here, so I only verified the persisted display path (which is what the
  demo shows).

---

## What changed under us: upstream #52/#53

While we were hardening, `origin/staging` advanced by two commits (a ~29k-line feature):

- **`#52` "build a policy workstream from scratch in the app"** delivered the entire
  `docs/specs/workstream-brain-live-build/` epic — all 5 stories (focal node, add-node +
  chunking, extract-concepts, add-edge, per-workstream live analysis).
- It introduced **per-workstream anchors** (`engine/ws_anchors.py`): analysis now
  resolves a document's anchors from `data/workstreams/<ws>/anchors/`, not the global
  `data/artifacts/anchor-index.json`. **This resolves the document-id namespace mismatch
  I flagged** — the reason live Analyze returned nothing — a different way than our plan
  assumed.
- The demo is **build-and-persist**: `open-finance-pd-2026` ships its own anchors, axes,
  and **findings** (47 + 32 on two edges), so the display works with no model call.
- It **hid** the legacy fixtures (`opres-v2`, `open-finance-ed`, `rmit-v2-2025` →
  `hidden:true`), so the sidebar shows only `open-finance-pd-2026`.
- **Demo date is 3 Aug** (the epic states it), not 5 Aug as our specs assumed.

**Consequence for our plan:** the artifact-integrity guard and the analyze fan-out cap
(Tier 1 specs B and C) hardened the shared-index live path the demo no longer uses —
**obsolete**. The cross-workstream climax (spec D) is the one piece #52 explicitly
**deferred** and for which no upstream spec exists — **still the only real gap.**

---

## What I did (all on `feat/demo-ready`, 7 commits over `origin/staging`)

| #   | Commit    | What                                                                                                                                                |
| --- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `ced8973` | Cherry-picked the demo-hardening specs + plan (kept for the record + the cross-climax spec).                                                        |
| 2   | `fa349ed` | CLAUDE.md correction — analyze/copilot are live, not "a fixture projection with no model seam" (the stale claim had misled an investigating agent). |
| 3   | `a118519` | `scripts/probe_live_analyze.py` — runs the real pipeline on a pair so you can judge live-analyze quality with creds.                                |
| 4   | `83dd73b` | Reconciled the docs with #52 (per-workstream anchors; marked specs B/C superseded; demo date → 3 Aug).                                              |
| 5   | `d321b43` | Fixed the hidden `opres-v2` fixture (pending findings, blank draft) — part of turning the 18 red tests green.                                       |
| 6   | `27be064` | Re-pointed the linkage-review tests to the surviving cross edge (they targeted a deleted fixture).                                                  |
| 7   | `fee630b` | **New demo-path smoke test** (`test_demo_smoke.py`) — the acceptance gate: walks the real `open-finance-pd-2026` display path. Needs no creds.      |

I also **dropped** three commits from the old `feat/demo-hardening-tier1` branch that
would have silently damaged the demo (they reverted upstream's intended state or
contradicted upstream tests). That old branch is left intact as history.

**Net effect: `feat/demo-ready` = current `origin/staging` + a green suite + a demo
smoke test + corrected docs + a feature backlog.** It brings `origin/staging` (currently
**18 tests red**) back to **green**.

---

## Demo-readiness assessment

**Verified working (persisted display path — what's shown on stage):**

- ✅ All 7 demo screens render with no errors (screenshots captured).
- ✅ Review screen shows 47 real findings with verbatim clause citations + labels.
- ✅ Graph, task, draft round-trip, cross-intelligence, review-queue.
- ✅ Full test suite green (engine 500, frontend 132).

**NOT verified — needs a machine with Azure credentials (I have none here):**

- ⚠️ **Live "Analyze"** on an unanalysed edge (2 of the 4 demo edges are unanalysed).
- ⚠️ **"Extract concepts"** on a node.
- ⚠️ **Copilot** chat.
- ⚠️ **Add-node with PDF upload + chunking** (interactive; not driven headless).
- These all matter only if you _click them live_ on stage. If the demo shows the
  pre-built `open-finance-pd-2026`, the persisted data covers it. **Rehearse these on a
  creds machine before 3 Aug** — use `scripts/probe_live_analyze.py` and the app itself.

**Minor cosmetic:** the workstream name renders "Open Finance PD **.** 2026" (a `·`
middot got mangled to a period in `open-finance-pd-2026/workstream.json`). One-character
data fix; not urgent.

---

## Decisions I need from you

1. **Merge `feat/demo-ready` → `staging`?** It's green, based on current staging, and
   fixes staging's 18 red tests. I did not push or merge. Recommended: yes (as a PR).
2. **Cross-workstream climax** (`x-open_finance_ed--opres_dp_2025`, 12 findings): it
   renders via a static fixture but points at _hidden_ legacy workstreams. Is it part of
   the 3 Aug demo, or is the demo just building `open-finance-pd-2026`? If it's in,
   `spec-cross-workstream-climax.md` is the spec to build from. If not, ignore it.
3. **Your feature ideas** (`backlog-copilot-drafting-flow.md`): most already ship (the 10
   node-metadata fields = `CONCEPT_FIELDS`; Copilot is already live Claude with streaming
   - citations; findings + maker-checker exist). The genuinely-new pieces are
     "recommendations" (turn an accepted finding into a concrete draft edit) and the staged
     `/brainstorm → /skeleton → /write → /deliver` lifecycle. **These are net-new product
     bets — do any belong in the 3 Aug demo, or are they post-hackathon?** One small,
     well-scoped gap: "add-node task type" exists at the _workstream_ level
     (`deliverable_type` = PD/ED/DP/Other) but not per-node, and lacks FAQ/slides.

---

## Recommended next steps (in order)

1. **You:** on a creds machine, rehearse the three live paths (Analyze, Extract-concepts,
   Copilot) against `open-finance-pd-2026` and run `probe_live_analyze.py` to confirm
   live-analyze quality. This is the only unverified demo risk.
2. **You:** decide merge (#1) and cross-climax scope (#2) above.
3. **Optional, small:** fix the "Open Finance PD · 2026" middot; add per-node task_type +
   FAQ/slides to the deliverable enum if you want it in the demo.
4. **Post-hackathon:** the drafting-lifecycle backlog, if you want it.

See also: `backlog-copilot-drafting-flow.md` (your captured ideas),
`spec-demo-hardening.md` (superseded, with pointers), `scripts/probe_live_analyze.py`.
