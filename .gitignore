# Internal / confidential BNM reference documents — kept local, never published
docs/references/

# macOS
.DS_Store

# Sensitive bank submissions — never committed
data/submissions/

# Raw ingested markdown — a per-build debug dump of what Document Intelligence
# produced (used to diagnose extraction issues). Rebuildable, not a frozen
# artifact; the committed contract is clause-index.json / graph.json only.
data/artifacts/_ingest/

# Large cross-jurisdiction benchmarking PDFs (HKMA/MAS/UK PRA/US/NZ Basel III
# source docs, ~80 MB). Kept local for the retrieval-pipeline experiment; this
# is a public repo, so they are never committed.
data/corpus/temp/

# Stray local build/tooling artifacts (the old Next.js web/ build tree removed
# on 16 Jul 2026; a 0-byte libghostty file). Not part of the tracked product.
web/
libghostty62

# Local secrets — copy .env.example to .env and fill in; never committed
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Claude Code local settings + agent worktrees (machine-specific)
.claude/

# graphify generated graphs (exploration artifacts, rebuildable, not source)
graphify-out/
graphify-out-corpus/

.agents
skills-lock.json
# subagent-driven-development scratch
.superpowers/
data/obsidian_vault/

# Experimental outputs (retrieval-ablation ARB spec) — top-level only,
# not scripts/experiments/ which holds the tracked runner scripts.
/experiments/
