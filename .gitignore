# Internal / confidential BNM reference documents — kept local, never published
docs/references/

# macOS
.DS_Store

# Sensitive bank submissions — never committed
data/submissions/

# Local secrets — copy .env.example to .env and fill in; never committed
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
